import React from 'react';
import {
  View,
  Switch,
  Text,
  SafeAreaView,
  Animated,
  Image,
  TouchableOpacity,
  FlatList,
  TextInput
} from 'react-native';

export const textinputList = () =>{
    return(
        <TextInput
        />

    )
}